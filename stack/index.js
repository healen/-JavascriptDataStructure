/** 一分钟演示，栈的应用* */const Stack = require("./Stack")const list = new Stack()/** 入栈* */for(let i=0;i<20;i++){  list.push(i)}console.log("操作：入栈0-19")console.log(`当前值:${list.peek()}`)console.log(`当前长度:${list.length()}`)console.table(list)/**出栈* */for(let i=0;i<10;i++){  list.pop()}console.log("操作：出栈9次")console.log(`当前值:${list.peek()}`)console.log(`当前长度:${list.length()}`)console.table(list)